/**
 * Canonical public origin (apex 307/308-redirects to www).
 *
 * Single source for every absolute self-URL — sitemap, robots, JSON-LD,
 * social-post generation, and the app registry all import from here so a
 * host change is a one-line edit.
 */
export const SITE_URL = "https://www.novique.ai";
